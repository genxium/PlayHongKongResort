# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET	/	controllers.Application.show()
POST /checkConnection controllers.Application.checkConnection()
POST /login controllers.Application.login()
POST /register controllers.Application.register()
POST /checkLoginStatus controllers.Application.checkLoginStatus()
POST /uploadingHandler controllers.Application.uploadingHandler()
POST /saveActivity controllers.Application.saveActivity()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
